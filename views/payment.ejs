<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment</title>

  <!-- Bootstrap and Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="/styles/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-50">

  <div class="container my-5">
    <h2 class="text-3xl font-bold text-center mb-5 text-gray-800">Confirm Your Course Selection</h2>
    
    <div class="bg-white p-5 rounded-lg shadow-lg max-w-md mx-auto">
      <h3 class="text-2xl font-semibold text-indigo-700 mb-4"><%= course.name %></h3>
      <p class="text-gray-600 mb-4"><%= course.description %></p>
      
      <div class="mb-4">
        <p class="text-lg font-semibold">Original Price: <span class="line-through">₹<%= course.original_price %></span></p>
        <% if (course.discount_percentage > 0) { %>
          <p class="text-lg font-semibold text-red-600">Discounted Price: ₹<%= course.discountedPrice %></p>
        <% } else { %>
          <p class="text-lg font-semibold">Price: ₹<%= course.original_price %></p>
        <% } %>
      </div>

      <form action="/verify-step-1" method="POST">
        <button type="submit" class="w-full py-2 mt-3 rounded-full bg-indigo-600 text-white font-bold hover:bg-indigo-700 transition-all duration-200 shadow-lg hover:shadow-xl">
          Proceed to Payment
        </button>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
